import React from 'react';
import './Toolbar.scss'
const Toolbar = () => {
    return (
        <nav className="toolbar__nav">
            <ul className="toolbar__ul">
                <li>Login</li>
            </ul>
        </nav>
    );
};

export default Toolbar;