import React from 'react';
import './Toolbar.css'
const Toolbar = () => {
    return (
        <nav className="toolbar__nav">
            <ul className="toolbar__ul">
                <li>Login</li>
            </ul>
        </nav>
    );
};

export default Toolbar;